-- union default carry a tag
Union type = union {
	i i32
	u u64
}

pub main fn(argc i32, argv *[u8]) i32 = {
	-- all values in zako will auto initialize to zero
	let a Union = {.i = 123}
	match a {
	.i(i) = { return i }
	-- never used
	.u(u) = { return 256 }
	}
	return 0
}
