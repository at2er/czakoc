write syscall 1(fd u32, buf [u8], len u64) i32

pub print_int fn(i i32) i32 =
	let mut x i32 = i
	let mut idx u32 = 7
	let mut arr [u8; 8] = {}
	while x > 0 then
		let q i32 = x / 10
		let r i32 = x - (10 * q)
		arr[idx] = r + 48 as u8
		idx -= 1
		x = q
	write 1, arr, 8
	return 0
